<Window x:Class="obhod.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="obhod" Height="300" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="True">
    
    <Grid Margin="20">
        <!-- Main Glass Container (Matching design) -->
        <Border CornerRadius="20" BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0D0D12" Offset="0"/>
                    <GradientStop Color="#0A0A10" Offset="0.5"/>
                    <GradientStop Color="#0E0C14" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#22FFFFFF" Offset="0"/>
                    <GradientStop Color="#0AFFFFFF" Offset="0.5"/>
                    <GradientStop Color="#18FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect Color="#000" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
            </Border.Effect>
            
            <Grid>
                <!-- Neon Text Content -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid>
                        <!-- Glow Layer -->
                        <TextBlock Text="OBHOD" FontSize="62" FontWeight="Black" Foreground="#FF5E00" Opacity="0.4" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <BlurEffect Radius="35"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <!-- Main Text -->
                        <TextBlock Text="OBHOD" FontSize="62" FontWeight="Black" Foreground="White" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#FF5E00" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Grid>
                    
                    <Border Background="#10FFFFFF" CornerRadius="4" Padding="10,2" Margin="0,5,0,0" HorizontalAlignment="Center">
                        <TextBlock Text="INITIALIZING SECURE ENGINE" FontSize="9" Foreground="#48FFFFFF" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <!-- Loading Animation at bottom -->
                <Border Height="2" Width="150" Background="#10FFFFFF" VerticalAlignment="Bottom" Margin="0,0,0,30" CornerRadius="1">
                    <Rectangle Height="2" Width="0" x:Name="ProgressRect" Fill="#FF5E00" HorizontalAlignment="Left">
                        <Rectangle.Effect>
                            <DropShadowEffect Color="#FF5E00" BlurRadius="10" ShadowDepth="0"/>
                        </Rectangle.Effect>
                    </Rectangle>
                </Border>
            </Grid>
        </Border>
    </Grid>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                    <DoubleAnimation Storyboard.TargetName="ProgressRect" Storyboard.TargetProperty="Width" 
                                   From="0" To="150" Duration="0:0:3.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseInOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
