<Window x:Class="obhod.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="obhod" Icon="pack://application:,,,/Resources/icon.ico"
        Height="640" Width="440"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="True"
        ResizeMode="CanMinimize">

    <Grid Background="Transparent">
        <Border x:Name="MainBorder" CornerRadius="20" BorderThickness="1" Margin="18">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0D0D12" Offset="0"/>
                    <GradientStop Color="#0A0A10" Offset="0.5"/>
                    <GradientStop Color="#0E0C14" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#22FFFFFF" Offset="0"/>
                    <GradientStop Color="#0AFFFFFF" Offset="0.5"/>
                    <GradientStop Color="#18FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="0" Opacity="0.9"/>
            </Border.Effect>

            <Grid>
                <!-- Animated glow pulse behind logo -->
                <Ellipse Width="220" Height="220" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-60,0,0">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#30FF5E00" Offset="0"/>
                            <GradientStop Color="#00FF5E00" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.3" To="0.8" Duration="0:0:2" AutoReverse="True">
                                                <DoubleAnimation.EasingFunction>
                                                    <SineEase EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- App Icon -->
                    <Image Source="pack://application:,,,/Resources/icon.ico" Width="64" Height="64" Margin="0,0,0,20" RenderOptions.BitmapScalingMode="HighQuality" HorizontalAlignment="Center">
                        <Image.Effect>
                            <DropShadowEffect Color="#FF5E00" BlurRadius="25" ShadowDepth="0" Opacity="0.6"/>
                        </Image.Effect>
                    </Image>
                    
                    <!-- Title with neon glow -->
                    <Grid>
                        <TextBlock Text="OBHOD" FontSize="52" FontWeight="Black" Foreground="#FF5E00" Opacity="0.5" HorizontalAlignment="Center" FontFamily="Segoe UI Variable,Segoe UI">
                            <TextBlock.Effect>
                                <BlurEffect Radius="25"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="OBHOD" FontSize="52" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" FontFamily="Segoe UI Variable,Segoe UI">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#FF5E00" BlurRadius="15" ShadowDepth="0" Opacity="0.7"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Grid>

                    <!-- Subtitle -->
                    <Border CornerRadius="6" Background="#08FFFFFF" BorderBrush="#10FFFFFF" BorderThickness="1" Padding="12,4" Margin="0,12,0,0" HorizontalAlignment="Center">
                        <TextBlock Text="bypass engine v1.2" FontSize="10" Foreground="#40FFFFFF" FontWeight="SemiBold" FontFamily="Segoe UI Variable,Segoe UI"/>
                    </Border>

                    <!-- Status dots animation -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,35,0,0">
                        <Ellipse x:Name="d1" Width="6" Height="6" Fill="#FF5E00" Margin="0,0,8,0" Opacity="0.2">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.2" To="1" Duration="0:0:0.5" BeginTime="0:0:0" AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <Ellipse x:Name="d2" Width="6" Height="6" Fill="#FF5E00" Margin="0,0,8,0" Opacity="0.2">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.2" To="1" Duration="0:0:0.5" BeginTime="0:0:0.2" AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <Ellipse x:Name="d3" Width="6" Height="6" Fill="#FF5E00" Opacity="0.2">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.2" To="1" Duration="0:0:0.5" BeginTime="0:0:0.4" AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                    </StackPanel>

                    <TextBlock x:Name="TxtInit" Text="Запускается..." FontSize="11" Foreground="#25FFFFFF" FontWeight="Medium" Margin="0,12,0,0" HorizontalAlignment="Center" FontFamily="Segoe UI Variable,Segoe UI"/>
                </StackPanel>

                <!-- Bottom branding -->
                <TextBlock Text="zapret engine · open source" Foreground="#10FFFFFF" FontSize="10" FontFamily="Segoe UI Variable,Segoe UI" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,18"/>
            </Grid>
        </Border>
    </Grid>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <ThicknessAnimation Storyboard.TargetName="MainBorder" Storyboard.TargetProperty="Margin" From="18,40,18,0" To="18,18,18,18" Duration="0:0:0.7">
                        <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </ThicknessAnimation.EasingFunction>
                    </ThicknessAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
